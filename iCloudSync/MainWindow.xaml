<Window x:Class="ICloudSync.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="iCloud Syncer" Height="650" Width="450" 
        Background="#121212" Foreground="White" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Resources>
                <Style TargetType="Border"><Setter Property="CornerRadius" Value="5"/></Style>
            </Style.Resources>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#3D3D3D"/></Trigger>
                <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.5"/></Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#007ACC"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#005A9E"/></Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

       <tb:TaskbarIcon x:Name="MyNotifyIcon" 
                        ToolTipText="iCloud Syncer"
                        IconSource="/app_icon.ico">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu Background="#1E1E1E" Foreground="White">
                    <MenuItem Header="Open Dashboard" Click="ShowApp_Click" />
                    <MenuItem Header="Sync Now" Click="SyncNow_Click" />
                    <Separator Background="#333"/>
                    <MenuItem Header="Exit Entirely" Click="ExitApp_Click" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
        
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="iCloud Folder" Foreground="#888" FontSize="11" Margin="0,0,0,5"/>
            <DockPanel Margin="0,0,0,12">
                <Button Content="Browse" DockPanel.Dock="Right" Style="{StaticResource ModernButton}" Click="BrowseICloud_Click"/>
                <TextBox x:Name="ICloudPathBox" Margin="0,0,10,0" Padding="8" Background="#1E1E1E" Foreground="White" BorderBrush="#333"/>
            </DockPanel>

            <TextBlock Text="Mirror Destination" Foreground="#888" FontSize="11" Margin="0,0,0,5"/>
            <DockPanel>
                <Button Content="Browse" DockPanel.Dock="Right" Style="{StaticResource ModernButton}" Click="BrowseSync_Click"/>
                <TextBox x:Name="SyncPathBox" Margin="0,0,10,0" Padding="8" Background="#1E1E1E" Foreground="White" BorderBrush="#333"/>
            </DockPanel>
        </StackPanel>

        <Border Grid.Row="1" Background="#1A1A1A" CornerRadius="8" Padding="15" Margin="0,10">
            <StackPanel>
                <CheckBox x:Name="StartupToggle" Content="Run at Startup" Foreground="White" Margin="0,0,0,10" Checked="StartupToggle_Changed" Unchecked="StartupToggle_Changed"/>
                <DockPanel LastChildFill="False">
                    <CheckBox x:Name="ScheduledSyncToggle" Content="Auto-Sync" VerticalAlignment="Center" Foreground="White" Checked="SaveConfig_Event" Unchecked="SaveConfig_Event"/>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <TextBlock Text="Interval (min):" VerticalAlignment="Center" Margin="10,0,5,0" Foreground="#888"/>
                        <TextBox x:Name="IntervalInput" Width="40" Text="30" Padding="3" Background="#252525" Foreground="White" BorderBrush="#444" TextChanged="SaveConfig_Event"/>
                    </StackPanel>
                </DockPanel>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="SyncNow" Content="Sync Now" Height="45" Margin="0,0,5,0" Style="{StaticResource AccentButton}" Click="SyncNow_Click"/>
                <Button x:Name="CancelSync" Grid.Column="1" Content="Cancel" Height="45" Margin="5,0,0,0" Style="{StaticResource ModernButton}" Click="CancelSync_Click" IsEnabled="False"/>
            </Grid>
            <ProgressBar x:Name="SyncProgressBar" Height="4" Margin="0,15,0,0" Background="#1E1E1E" Foreground="#007ACC" BorderThickness="0"/>
            <TextBlock x:Name="StatusText" Text="Status: Idle" HorizontalAlignment="Center" Margin="0,10,0,0" Foreground="#007ACC"/>
        </StackPanel>

        <StackPanel Grid.Row="3" VerticalAlignment="Bottom">
            <Border Background="#1E1E1E" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="STORAGE SUMMARY" FontSize="10" FontWeight="Bold" Foreground="#666" Margin="0,0,0,10"/>
                    <DockPanel>
                        <TextBlock Text="Total Size:" Foreground="#AAA"/>
                        <TextBlock x:Name="TotalSizeText" Text="---" HorizontalAlignment="Right" Foreground="White"/>
                    </DockPanel>
                    <DockPanel Margin="0,5,0,0">
                        <TextBlock Text="Photos Only:" Foreground="#AAA"/>
                        <TextBlock x:Name="PhotoSizeText" Text="---" HorizontalAlignment="Right" Foreground="#007ACC" FontWeight="Bold"/>
                    </DockPanel>
                </StackPanel>
            </Border>
            <DockPanel Margin="5,0">
                <TextBlock x:Name="FileCountText" Text="Files: 0" Foreground="#666" FontSize="11"/>
                <TextBlock x:Name="LastSyncText" Text="Last Sync: Never" HorizontalAlignment="Right" Foreground="#666" FontSize="11"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</Window>